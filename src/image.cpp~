Imagem::Imagem(unsigned int nLargura = 0, unsigned int nAltura = 0) :
    largura(nLargura), altura(nAltura) {
    // Allocate pixel vector of image
    pixeis = new CorRGB[largura * altura];
}

Imagem::~Imagem(){
    delete[] this->pixeis;
}
